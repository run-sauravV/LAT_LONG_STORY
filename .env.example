OPENWEATHER_API_KEY=
EE_PROJECT_ID=
# Servers (Render): paste the full GCP service-account JSON key here for Earth Engine.
# Without it, GIS/satellite fall back to labeled mock data.
EE_SERVICE_KEY_JSON=
OPENTOPO_API_KEY=
# Optional model overrides (defaults to <root>/water_best.pt and <root>/best.pt)
WATER_MODEL_PATH=./water_best.pt
VEGETATION_MODEL_PATH=./best.pt
